---
interface Props {
  items: readonly string[];
}

const { items } = Astro.props;
---

<ul class="space-y-3 rounded-lg border border-stone-200 bg-white p-6 text-stone-700">
  {items.map((item) => <li>{item}</li>)}
</ul>
